const https=require('https');
const {db}=require('../db/db');
const THRESHOLD=Number(process.env.QUESTION_SIMILARITY_THRESHOLD||0.85);
function postJson(url,body,headers={}){return new Promise((resolve,reject)=>{const u=new URL(url);const req=https.request({hostname:u.hostname,port:u.port||443,path:u.pathname+u.search,method:'POST',headers:{'Content-Type':'application/json',...headers}},res=>{let data='';res.on('data',c=>data+=c);res.on('end',()=>{try{const json=JSON.parse(data);if(res.statusCode<200||res.statusCode>=300)return reject(new Error(json.error?.message||json.error||`Embedding request failed (${res.statusCode})`));resolve(json);}catch(e){reject(new Error('Invalid embedding response'));}})});req.on('error',reject);req.setTimeout(30000,()=>{req.destroy();reject(new Error('Embedding request timed out'));});req.write(JSON.stringify(body));req.end();});}
function cosine(a,b){let dot=0,aa=0,bb=0;for(let i=0;i<a.length;i++){dot+=a[i]*b[i];aa+=a[i]*a[i];bb+=b[i]*b[i];}return aa&&bb?dot/(Math.sqrt(aa)*Math.sqrt(bb)):0;}
async function embed(text){const key=process.env.LLM_API_KEY;if(!key)return null;const base=(process.env.LLM_BASE_URL||'https://api.openai.com/v1').replace(/\/$/,'');const model=process.env.EMBEDDING_MODEL||'text-embedding-3-small';const r=await postJson(`${base}/embeddings`,{model,input:text},{Authorization:`Bearer ${key}`});return r.data?.[0]?.embedding||null;}
async function findSimilarQuestions({title,description,excludeId=null}){const text=`${title}\n${description||''}`.trim();const vector=await embed(text);if(!vector)return {configured:false,threshold:THRESHOLD,matches:[]};const rows=db.prepare(`SELECT id,title,description,problem_statement FROM questions WHERE status='published' AND is_active=1 ${excludeId?'AND id<>?':''}`).all(...(excludeId?[excludeId]:[]));const matches=[];for(const q of rows){const qv=await embed(`${q.title}\n${q.description||q.problem_statement||''}`);if(!qv)continue;const similarity=cosine(vector,qv);if(similarity>=THRESHOLD)matches.push({id:q.id,title:q.title,similarity:Number(similarity.toFixed(4))});}return {configured:true,threshold:THRESHOLD,matches:matches.sort((a,b)=>b.similarity-a.similarity)};}
module.exports={findSimilarQuestions,THRESHOLD};
