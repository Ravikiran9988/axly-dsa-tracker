const express=require('express');const router=express.Router();const {authenticate}=require('../middleware/auth');const {ensurePracticeSchema}=require('../db/practiceSchema');const {seedPracticeProblems}=require('../db/practiceSeed');const c=require('../controllers/practiceController');ensurePracticeSchema();if(process.env.NODE_ENV!=='test')seedPracticeProblems();router.use(authenticate);router.get('/problems',c.problems);router.get('/topics',c.topics);router.get('/patterns',c.patterns);module.exports=router;
